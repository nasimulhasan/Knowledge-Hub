rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global Safety Net - allow public read, and write for this simplified demo
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
